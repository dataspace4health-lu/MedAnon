[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "medanon-core"
version = "0.1.0"
description = "MedAnon domain types, port contracts, and analytics modules"
requires-python = ">=3.12"
dependencies = []
