apiVersion: v2
name: medanon
description: >
  Full FHIR de-identification and pseudonymization stack — anonymizer,
  HAPI FHIR R4 server, gPAS TTP service, and React browser UI.
  Umbrella chart that composes the four standalone sub-charts.
type: application
version: 0.1.0
appVersion: "2025.2.0"
keywords:
  - fhir
  - de-identification
  - pseudonymization
  - gpas
  - healthcare
dependencies:
  - name: anonymizer
    version: "0.1.0"
    repository: "file://../charts/anonymizer"
  - name: fhir-server
    version: "0.1.0"
    repository: "file://../charts/fhir-server"
  - name: gpas
    version: "0.1.0"
    repository: "file://../charts/gpas"
  - name: ui
    version: "0.1.0"
    repository: "file://../charts/ui"
  # Opt-in analytics microservice sub-chart.
  # Enable with: --set analytics.enabled=true (or in values.yaml)
  - name: analytics
    version: "0.1.0"
    repository: "file://../charts/analytics"
    condition: analytics.enabled
