apiVersion: v2
name: analytics
description: >
  MedAnon Analytics microservice — lightweight container for risk analysis and
  synthetic data generation. No gPAS, Presidio, or fhirpathpy dependencies.
  Enable via the medanon umbrella chart with analytics.enabled=true.
type: application
version: 0.1.0
appVersion: "1.0.0"
