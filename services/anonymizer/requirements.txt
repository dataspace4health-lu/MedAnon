# Security note: exact pins prevent silent introduction of vulnerable transitive
# dependencies. Update pins deliberately and review changelogs for security fixes.
fastapi==0.135.1
pydantic==2.12.5
uvicorn==0.42.0
PyYAML==6.0.3              # CVE history in older versions — always pin exactly
fhirpathpy==0.1.0
rich==14.3.3
pycryptodome==3.23.0       # Crypto library — always pin exactly
python-dateutil==2.9.0.post0
defusedxml==0.7.1           # Security XML parser — always pin exactly
prometheus-client==0.24.1
slowapi==0.1.9
httpx==0.28.1              # FHIR client HTTP requests
urllib3==2.3.0             # FHIR client connection pooling

# NLP-based PHI/PII detection
presidio-analyzer==2.2.361
presidio-anonymizer==2.2.361
spacy==3.8.11

# install the spaCy model separately:
#   python -m spacy download en_core_web_lg