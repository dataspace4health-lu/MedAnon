"""formats — Non-FHIR data format adapters (CDA, HL7v2, DICOM).

Each module provides ``parse()`` / ``serialize()`` helpers that convert
format-specific payloads into FHIR resource dicts and back.
"""
