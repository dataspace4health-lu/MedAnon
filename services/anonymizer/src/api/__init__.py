"""api  FastAPI application layer.

Sub-packages:
    routers/     HTTP endpoint handlers (one file per resource group)
    schemas/     Pydantic request/response models
    services/    Business-logic layer called by routers
"""
