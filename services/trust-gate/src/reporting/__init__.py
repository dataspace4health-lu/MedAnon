"""Reporting layer: render a QualityPassport into human-readable formats.

The domain model (passport.py) holds data only; rendering lives here so the model
does not depend on presentation. Depends on the model, never the reverse.
"""
